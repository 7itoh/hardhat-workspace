{
  "name": "src",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "yarn workspace hardhat_dev test",
    "dev": "run-p dev:*",
    "dev:run-localchain": "yarn workspace hardhat_dev localchain",
    "dev:frontend": "wait-on http://localhost:8545 && yarn workspace frontend_dev dev"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/frontdev",
      "**/hardhatdev/**"
    ]
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "ts-generator": "^0.1.1",
    "ts-node": "^10.3.0",
    "typescript": "^4.4.4",
    "wait-on": "^6.0.0"
  }
}
